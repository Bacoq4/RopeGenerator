using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HairHead : HairBase
{
    protected override void Update()
    {
        //  Vector3 correctedPos = new Vector3(nextStack.position.x, nextStack.position.y , nextStack.position.z);
        //  transform.position = correctedPos;
    } 
    
    // hairhead in pozisyonunu kendimiz ayarlıyabilmeliyiz.
}
